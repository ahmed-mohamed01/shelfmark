# Local development - builds from source with debug enabled
services:
  shelfmark-dev:
    extends:
      file: ./compose/docker-compose.yml
      service: shelfmark
    build:
      context: .
      dockerfile: Dockerfile
      target: shelfmark
    cap_add:
      - SYS_PTRACE
    environment:
      DEBUG: true
      # HIDE_LOCAL_AUTH: true
      OIDC_AUTO_REDIRECT: true
      # Uses host's TZ env var if set (e.g. exported in your shell), otherwise falls back to Europe/London.
      TZ: ${TZ:-Europe/London}
    volumes:
      - ./.local/config:/config
      - ./.local/books:/books
      - ./.local/log:/var/log/shelfmark
      - ./.local/tmp:/tmp/shelfmark
      - ./shelfmark:/app/shelfmark:ro
      - ./frontend-dist:/app/frontend-dist:ro
      # Required for torrent / usenet - path must match your download client's volume exactly
      # - /path/to/downloads:/path/to/downloads
